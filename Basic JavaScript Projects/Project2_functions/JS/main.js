//a function taht uses the += operator
function MyFunction() {
    var str1 = "Hello!"; //the variable
    str1 += " This is my first function!"; //using += to add to the variable
    document.getElementById("Button_Text").innerHTML = str1; // using document.getElementById()
}
